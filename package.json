{
  "name": "gachadex",
  "private": true,
  "version": "0.1.0",
  "description": "GachaDex — leveraged perpetual-futures exchange on Pokémon-card prices (play-money MVP)",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @pokex/web dev",
    "dev:api": "pnpm --filter @pokex/api dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "dependencies": {
    "react-router-dom": "^7.17.0"
  }
}
